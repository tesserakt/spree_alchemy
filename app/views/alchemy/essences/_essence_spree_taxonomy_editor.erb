<div class="content_editor essence_spree_taxonomy">
<label><%= render_content_name(content) %></label>
  <%= select_tag(
    content.form_field_name,
    options_from_collection_for_select(Spree::Taxonomy.all, :id, :name, content.essence.taxonomy_id),
    :class => ['alchemy_selectbox very_long', html_options[:class]].join(' '),
    :style => html_options[:style]
) %>
</div>
